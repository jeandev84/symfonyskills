nelmio_api_doc:
    documentation:
        info:
            title: API Books Publisher
            description: API for publishing books and more
            version: 1.0.0
    areas: # to filter documented areas
        path_patterns:
            #- ^/api(?!/doc$) # Accepts routes under /api except /api/doc
            - ^/api/v1 # Accepts routes under /api except /api/doc
